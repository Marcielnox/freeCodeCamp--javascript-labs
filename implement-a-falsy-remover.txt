** start of script.js **

function bouncer(arr) {
  let filteredArray = []; 

  for (let i = 0; i < arr.length; i++) {
    if (arr[i]) {
      filteredArray.push(arr[i]);
    }
  }
  return filteredArray;
}

** end of script.js **

