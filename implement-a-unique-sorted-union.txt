** start of script.js **

function uniteUnique(...arrays) {
  const result = [];
  for (let arr of arrays) {
    for (let num of arr) {
      if (!result.includes(num)) {
        result.push(num);
      }
    }
  }

  return result;
}

** end of script.js **

